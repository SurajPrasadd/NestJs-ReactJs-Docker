2. approveOrReject if below level is pending mark as approved in case of approved 
3. create a contract using purchase_requests (upload contract- cost BusinessProduct)
4. check cart and update cost in case of contract 
5. only send PR where contract not avalbel else order
6. Order from contract before expire date


Run backend :nest start --watch

Run frontend :npm run dev

docker ps => List running containers

Docker:
docker-compose up -d
docker compose down

Rebuid :
docker compose down
docker compose up -d --build

Container Terminal :
docker exec -it backend bash
docker exec -it frontend bash

Execute the SQL file
cat ./db/init.sql | docker exec -i postgres psql -U postgres -d appdb

TRUNCATE TABLE public.categories,
               public.business,
               public.catalogs,
               public.pricing,
               public.contracts
RESTART IDENTITY CASCADE;


Connect To postgres DB:
docker exec -it <container name>  psql -U postgres -d appdb
\dt

List of TABLE:
SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';


# Build and remove storage
docker compose down -v


const backendUrl = this.config.get<string>('BACKEND_URL', '');
    const updatedRecord: BusinessProduct = {
      ...record,
      product: {
        ...record.product,
        images:
          record.product?.images?.map((img) => ({
            ...img,
            imageUrl: `${backendUrl}${img.imageUrl}`,
          })) || [],
      },
    };

